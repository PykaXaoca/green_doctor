org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# This newDsl flag was added by the Flutter template
android.newDsl=false

# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Отключает валидацию ресурсов, которая падает на отсутствующем
# R-def.txt в некоторых плагинах при AGP 8.x.
android.disableResourceValidation=true

# Отключает проверку соответствия JVM target между Java и Kotlin задачами.
# Некоторые плагины (sentry_flutter, tflite_flutter) собираются с Java 1.8
# или 11, тогда как основной проект использует Java 17. Эта проверка
# генерирует ошибку "Inconsistent JVM-target compatibility", хотя
# компиляция в байткод проходит корректно.
# Официальный флаг Kotlin: https://kotl.in/jvm-target-validation
kotlin.jvm.target.validation.mode=IGNORE